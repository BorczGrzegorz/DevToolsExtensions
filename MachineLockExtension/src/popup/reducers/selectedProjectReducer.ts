import { ActionType } from "../actions/models";
import { Action } from "redux";

export const selectedProjectReducer = (previousState: string = '', action: Action<ActionType>) => {
    return previousState;
}